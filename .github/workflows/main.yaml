name: Ephemeral Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, linux, ephemeral]
    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Running on ephemeral runner jou"
          hostname
